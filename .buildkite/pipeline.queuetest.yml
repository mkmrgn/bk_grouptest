steps:
  - label: build
    branches: "!master"
    commands:
      - /bin/bash annotate.sh
      - wait: ~
  - label: test release
    branches: "!master"
    commands:
      - /bin/bash echo "lol"
      - wait: ~
agents:
  queue: tester
